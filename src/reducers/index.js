// Node modules import

import {combineReducers} from 'redux'

// Stage hodling in combine reducers

const rootReducer = combineReducers ({

    state: (state = {}) => state
});

export default rootReducer
